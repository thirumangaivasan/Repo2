name: Cypress Tests
on: [push]
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.7.0
        
      - name: Cypress run
        uses: cypress-io/github-action@v5.6.1
       

   
 
 
